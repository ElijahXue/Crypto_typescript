export type Crypto = {
    id: string,
    symbol: string,
    name: string,
    current_price: number,
    market_cap: number,
    total_volume: number,
    high_24h: number,
    low_24h: number,
    total_supply: number,
    owned: number
  }